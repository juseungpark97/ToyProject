package com.kh.toy.repository;

import com.kh.toy.model.SupportConditions;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportConditionsRepository extends JpaRepository<SupportConditions, Long> {
}
